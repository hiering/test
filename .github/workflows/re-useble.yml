name: Reusable workflow

on:
  workflow_call:
    inputs:
      time:
        default: 1m

jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep
        run: sleep ${{ inputs.time }}
