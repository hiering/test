name: Test

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  job1:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.step1.outputs.test }}
    steps:
      - name: Sleep
        run: sleep 5m


    
      
